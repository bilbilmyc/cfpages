# Soren 工作台设计规范

用途：访客直接使用工具，站主维护文章和图片。工具优先，中文、紧凑、明确反馈。

## 页面结构
- 首页：我的文件（连接后按最近打开排列）+ 工具目录 + 可操作的快速 JSON 区域 + 最近文章。
- 工具：全宽操作工作区，功能说明一行，工具栏和编辑区可见。
- 文章：列表与搜索；阅读区限制行宽，不压缩正文。
- 管理：左侧文章索引，右侧编辑；发布与保存草稿始终分开。
- 顶部导航取代旧侧栏，保持所有原 URL。工具有独立入口和当前状态。

## 视觉
modern-minimal / Cobalt：冷白底、深灰文字、蓝色强调，6px 控件圆角。
标题使用 Segoe UI Variable Display / Microsoft YaHei，正文 Segoe UI / Microsoft YaHei，代码 Cascadia Code / Consolas。
所有颜色和字体来自 tokens.css；4px 间距尺度。无装饰插图、渐变、宣传标语或虚构统计。
动效仅按钮按压；聚焦边框立即出现。尊重 reduced motion。

## 交互
用户可区分加载、空数据、失败与完成。错误提供重试。搜索和工具选择可通过 URL 分享。
功能按键使用动词；图标按钮提供中文名称。手机工具栏换行，编辑区不被页面横向溢出裁切。
管理员密钥仅内存存放；保留未保存内容提醒、恢复副本、导出及版本冲突提示。
