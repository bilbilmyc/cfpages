# 多文档发布记录

## 变更与验证

首页新增站主文件库，流程图和画布可以分别新建、命名、复制、搜索、归档、恢复，按最近打开排序。文件使用独立 URL，连接同一站点后可跨设备继续编辑。

内容和名称修改使用数据库 revision 条件更新。串行自动保存防止慢请求打乱顺序，409 冲突停止自动保存。每个文件、编辑窗口有独立恢复副本；恢复旧内容需要显式选择，云端版本变化时只能另存副本。密钥只在内存中。

本地 `npm run check`：33 项单元测试通过，生产构建通过。`PLAYWRIGHT_CHANNEL=chrome npm run test:browser`：17 项浏览器/API 验收通过，覆盖文件生命周期、独立复制、归档恢复、分页筛选、最近打开、两个浏览器上下文继续编辑和版本冲突、慢请求串行保存、失败后的恢复、临时草稿转文件，以及既有文章、图片和工具回归。窄屏文件列表在 320/768/1440 像素验证。

本轮代码审查覆盖鉴权、绑定参数 SQL、请求/文件大小限制、并发写入、卸载取消、恢复副本清理、原有数据兼容及分页。没有新增依赖。修正了列表分页重复条目、复制名称截断代理字符和重命名测试定位。

## 迁移与兼容

`0004_tool_documents.sql` 仅新增 `tool_documents` 表和最近打开索引。预览先执行，真实接口验收通过后再在生产执行；不修改 `images`、`posts`、`tool_drafts`。

首次连接文件库通过受保护接口复制两种旧云草稿，固定导入 ID 并使用 `ON CONFLICT DO NOTHING`，不会覆盖已编辑文件。格式不兼容时跳过并提示，旧草稿保留。后续旧草稿与新文件独立保存。

控制台手动执行 SQL 不会注册 Wrangler 迁移账本；两者要分别核查。新的本地预览会按文件顺序执行全部迁移。

## 回退

上线前生产基线为 `59bb03911826da0c44c6eab9144e6a9d3ada038a`，成功部署 `8156b7ae-ba99-4fee-9e94-92e6913a609d`。应用异常时在 Pages 选择该成功生产部署回滚，随后通过 Git revert 修复自动部署分支。

回退保留 `tool_documents` 和原有三张表，不执行 DROP 或反向同步。旧应用暂时无法打开新命名文件，但新数据仍保留，可恢复新版后继续使用。旧云草稿仍可读写。Pages 回退不回退 D1/R2 数据；Time Travel 只用于已确认的数据损坏，先暂停写入并备份当前数据。

云端部署、备份书签与验收结果将在实际执行后追加，不能将本地通过当成远程验收完成。

## 预览验收（2026-09-19）

- 功能提交：`aa37149c6dcef69df4f7c5539f86d75c644ad06c`。
- Cloudflare 部署 `ae2353d3-0902-43cc-ba1b-38dc05c1aad0`，唯一 URL <https://ae2353d3.cfpages-7s6.pages.dev>；GitHub 检查确认部署成功。
- 独立预览数据库 `1fec44f7-5554-4fbf-8762-40d1285cde8b` 控制台执行 0004，查询确认 `tool_documents` 表和 `idx_tool_documents_recent` 索引存在。
- 只对该唯一预览 URL 使用独立预览密钥执行完整文件 API 测试：鉴权、跨站拒绝、输入验证、独立复制、并发写入仅一方成功、旧版本拒绝、归档/恢复、打开不影响内容版本、筛选分页和一次性旧草稿导入均通过；新建测试文件最终归档。
- [GitHub Actions 35447390285](https://github.com/bilbilmyc/cfpages/actions/runs/35447390285) 在 Linux 中完成 33 项单元测试、17 项浏览器/API 验收，全部通过。
